// This program outputs a message to screen
#include<iostream> // This is an include directive
#include<string>
#include<vector>
#include<algorithm>
#include<cmath>
using namespace std;

int main() { // main is the entry point to the program
    std::cout << "Hello, programming!\n"; // Actually writes to the screen
    std::cout << "Here we go!" << '\n';
    // cout : pronounced see-out.
    return 0; // Return code is optional
}
